name: Build AutoDial

on:
  push:
    branches: [master]

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Decode Keystore
        working-directory: AutoDial/android/app
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > autodial-release.p12
          echo "KEYSTORE_FILE=AutoDial/android/app/autodial-release.p12" >> $GITHUB_ENV
          echo "KEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> $GITHUB_ENV
          echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
          echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}" >> $GITHUB_ENV

      - name: Create gradle wrapper
        working-directory: AutoDial/android
        run: |
          gradle wrapper --gradle-version 8.4

      - name: Build Debug APK
        working-directory: AutoDial/android
        run: ./gradlew assembleDebug

      - name: Build Release APK
        working-directory: AutoDial/android
        run: ./gradlew assembleRelease

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: AutoDial-APK-Debug
          path: AutoDial/android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: AutoDial-APK-Release
          path: AutoDial/android/app/build/outputs/apk/release/app-release.apk
